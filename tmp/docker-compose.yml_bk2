# ファイルフォーマットのバージョン
version: '3'

# サービスという単位でコンテナを管理
services:
    # 各サービスの名前を定義。
    # サービス名はかなり重要で、他のコンテナからこのサービス名でアクセスできたり、
    # 起動の順番を定義する。
    httpd:
        # PullするDockerイメージを指定
        image: httpd:2.4.43
        # localhostの80番ポートあての通信をコンテナの80番ポートに転送する設定
        ports:
            - "80:80"
